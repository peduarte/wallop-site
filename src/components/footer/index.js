import './style.css';

import React from 'react';

import Infobar from '../info-bar';

export default React.createClass({
  render() {
    return (
      <footer>
        <Infobar />
      </footer>
    )
  }
});
